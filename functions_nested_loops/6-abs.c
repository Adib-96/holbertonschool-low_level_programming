#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @x: The number to be checked.
 * Return: integer number with conversion
 * from negative to positive.
 */

int _abs(int x)
{
if (x < 0)
{
return (-x);
}
else
{
return (x);
}
}

